<template>
  <div class="post">
    <div class="post-header">
      <div
        class="profile"
        :style="`background-image: url(${post.userImage}&random=${Math.random()})`"
      ></div>
      <span class="profile-name">{{ post.name }}</span>
    </div>

    <div
      class="post-body"
      :style="`background-image: url(${post.postImage}&random=${Math.random()})`"
    ></div>

    <div class="post-content">
      <p>{{ post.likes }} Likes</p>
      <p><strong>{{ post.name }}</strong> {{ post.content }}</p>
      <p class="date">{{ post.date }}</p>
    </div>
  </div>
</template>

<script>
export default {
  name: 'Post',
  props: {
    post: Object
  }
};
</script>